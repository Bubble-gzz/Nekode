using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DrBubble : MonoBehaviour
{
    // Start is called before the first frame update
    public static DrBubble instance;
    void Awake()
    {
        instance = this;
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
